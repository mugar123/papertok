# Lo abierto de Semantic Scholar: caché compartida y un solo dueño de las devoluciones — plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Cerrar S6 (la caché de borde partida por origen) y las cinco fugas de cuota reales de `cacheResponse` y `reserveOpenAlexBudget`, con un mecanismo cada una en vez de un parche por síntoma.

**Architecture:** Para la caché, la clave pierde `_origin` y un `serveCached` reconstruye la única cabecera que variaba por origen en el momento de servir; seis llamadores, un helper. Para las fugas, cambia el contrato de las puertas: cada una devuelve *lo que reservó* en vez de un veredicto, `cacheResponse` acumula los asientos aceptados y es el **único** que los devuelve — ante un rechazo o un `throw` de una puerta posterior — y el fetcher queda fuera de ese `try` a propósito, porque un fetch que falla puede haber sido un envío. `reserveOpenAlexBudget` devuelve por dentro el minuto cuando el día rechaza.

**Tech Stack:** Cloudflare Worker (`worker/report-api.js`, Durable Object `RequestQuotaLedger`), `node --test`.

**Spec:** `docs/AUDITORIA-S2-ABIERTOS-2026-09-03.md`. Cubre C1 (S6) y L1–L6 salvo **L2c**, que la auditoría reclasifica como *no fuga* (§2, «La que no es fuga»); una tarea de este plan **prueba** esa decisión en vez de dejarla implícita.

## Global Constraints

- Comentarios de código, mensajes de commit y `worker/README.md` **en inglés** (`worker/AGENTS.md`); prosa de `STATE.md` en español.
- **Cada test nuevo muere por mutación**: la mutación se **ejecuta** y su salida en rojo va al informe. Reasonar que «moriría» no es evidencia; tres tareas de un plan anterior volvieron por esto.
- **No sondear producción.** Ni `api.papertok.app` ni ningún proveedor. Nada aquí necesita la red.
- **El fetcher no devuelve cuota.** Un fallo tras el fetcher puede haber sido un envío al proveedor (un timeout es un envío sin respuesta), y el ledger cuenta envíos. Cualquier `try` que devuelva asientos termina **antes** del fetcher. Es una decisión de la auditoría, y Task 3 la pincha con un test.
- Baseline: `npm test` en `main` a `a717b06` da **1977/1977**; `worker/report-api.test.js` tiene **102**. Ejecutar en un worktree desde `main` y copiar `.env.local` a mano (sin él un test del Worker se cuelga en vez de fallar). Si `main` ha avanzado al empezar, medir y ajustar los recuentos: son bookkeeping, no fallos.
- No se tocan `wrangler.toml`, `worker/request-quota-ledger.js`, `worker/upstream-pace.js` ni nada bajo `src/`.
- `S2_GLOBAL_MINUTE_LIMIT`, `PROVIDER_*_MINUTE_LIMIT`, `OPENALEX_GLOBAL_*_LIMIT` y todos los `retry-after` se quedan como están. Este plan no cambia ningún número que vea un cliente; cambia **quién devuelve** y **qué se comparte**.
- **Fixtures que ya existen y se reutilizan**, en `worker/report-api.test.js`: `withWorkerFetchMock` (:7), `AUTHENTICATED_ENV` (:927), `withCachedIdentity` (:937), `OPEN_ROUTE_ENV` (:1523), `countingQuotaLedger` (:1530), `actionRecordingLedger` (:2056), `TRENDS_BASE` y `SIGNED_IN` (:1131-1134), `trendsPage` (:1136). Comprobar los números de línea con `grep -n` antes de fiarse; el fichero crece.
- Si esto se despliega, **el Worker antes que el frontend**, como desde el 03-09. Aquí el frontend no cambia, así que solo hay una pieza.
- Trailer de commit exacto: `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`

---

## Mapa de ficheros

| Fichero | Tareas | Qué cambia |
|---|---|---|
| `worker/report-api.js` | 1, 2, 3, 4 | `serveCached` + `canonicalCacheKey` sin origen (6 sitios); `reserveProtectedProviderQuota` y `reserveOpenAlexBudget` devuelven asientos; `reserveGates` + `releaseHeld`; `awaitSharedPace` deja de devolver; el comentario de OpenAlex deja de justificar una fuga |
| `worker/report-api.test.js` | 1, 2, 3, 4 | un test de caché compartida; `scriptedQuotaLedger` + `assertRefunded`; un test por fuga; el test que pincha «el fetcher no devuelve» |
| `STATE.md`, `worker/README.md` | 5 | la entrada de esta pasada, la reclasificación de L2c, y una frase en el README sobre caché compartida y devoluciones |

**Orden:** 1 es independiente. 2 → 3 → 4 van en orden (3 envuelve lo que 2 escribe; 4 cambia una línea que 2 dejó). 5 al final.

---

### Task 1: Una entrada de caché sirve a todos los orígenes (C1 / S6)

**Files:**
- Modify: `worker/report-api.js` — `canonicalCacheKey` (:300-308) y sus seis llamadores (:494, :971, :2005, :2349, :2386, :2405)
- Test: `worker/report-api.test.js`

**Interfaces:**
- Produces: `canonicalCacheKey(request, canonicalParams = {})` (pierde `origin`); `serveCached(cached, origin, env)` → `Response`. Ninguna tarea posterior los toca.

**Por qué.** La Cache API de Workers ignora `Vary`, y la respuesta cacheada lleva `access-control-allow-origin` cocido; `_origin` en la clave era la forma de no servir la cabecera de un origen a otro. El cuerpo es idéntico para todos (misma URL upstream, mismos parámetros canónicos), así que la entrada puede compartirse si la cabecera se reconstruye al servir. Donde duele hoy: `/health/scopus` cuesta una llamada del **cupo semanal** de Elsevier y se cachea para protegerlo; un monitor sin `Origin` y un navegador son dos entradas y dos llamadas por una respuesta idéntica.

- [ ] **Step 1: El test**

Añadir a `worker/report-api.test.js`, después del test `keeps two different PubMed queries in two different cache entries`:

```js
// The body of a cached answer is the same whoever asks -- same upstream URL,
// same canonical parameters -- and the only thing that ever differed by origin
// was the CORS header stored with it. Keying on the origin protected that
// header at the price of one upstream call per origin: for `/health/scopus`,
// which is cached precisely to guard a weekly provider allowance, a monitor
// without an `Origin` and a browser with one were two calls for one answer.
test('one cached answer serves every allowed origin, each with its own CORS header', async () => {
  const stored = new Map();
  let upstreamCalls = 0;
  const originalFetch = globalThis.fetch;
  const originalCaches = globalThis.caches;
  globalThis.fetch = async () => {
    upstreamCalls += 1;
    return new Response('{"data":[]}', { headers: { 'content-type': 'application/json' } });
  };
  globalThis.caches = {
    default: {
      match: async request => stored.get(request.url)?.clone() || null,
      put: async (request, response) => stored.set(request.url, response.clone()),
    },
  };
  try {
    const url = 'https://papertok-report-api.example/sources/s2?q=malaria';
    const ask = headers => reportApi.fetch(new Request(url, { headers }), OPEN_ROUTE_ENV);

    const bare = await ask({});
    const apex = await ask({ origin: 'https://papertok.app' });
    const www = await ask({ origin: 'https://www.papertok.app' });

    assert.equal(upstreamCalls, 1, 'three askers, one provider call');
    assert.equal(stored.size, 1, 'three askers, one entry');
    assert.equal(bare.headers.get('access-control-allow-origin'), null, 'no Origin, no CORS header');
    assert.equal(apex.headers.get('access-control-allow-origin'), 'https://papertok.app');
    assert.equal(www.headers.get('access-control-allow-origin'), 'https://www.papertok.app');
    // Intermediate and browser caches must not cross-serve either.
    assert.equal(www.headers.get('vary'), 'Origin');
    assert.deepEqual(await www.json(), { data: [] }, 'the shared body is the body');
  } finally {
    globalThis.fetch = originalFetch;
    if (originalCaches === undefined) delete globalThis.caches;
    else globalThis.caches = originalCaches;
  }
});
```

- [ ] **Step 2: Verlo fallar**

Run: `node --test worker/report-api.test.js 2>&1 | grep -E "^(✖|ℹ fail)"`
Expected: `fail 1` — `upstreamCalls` es `3` (o `stored.size` es `3`).

- [ ] **Step 3: `serveCached` y la clave sin origen**

Sustituir `canonicalCacheKey` entera (con su comentario) por:

```js
// The key is built from the values the handler is about to send upstream, never
// from the ones that arrived. Those are two different things everywhere in this
// file -- `limit=99` is clamped to 10, `categories=zzz` is dropped, a DOI is
// lowercased, a filter is trimmed -- and keying on the raw ones made every
// variant of a discarded parameter a fresh miss for one identical upstream call.
// On the routes OpenAlex bills that was money: `categories=zzz1`, `zzz2`, ...
// each cost two calls against a $1/day budget.
//
// The contract this puts on callers is the reverse of the old parameter
// allowlist, and stricter: whatever affects the answer must appear here, because
// nothing else does. Anything absent is not merely un-keyed, it is shared.
//
// The origin is deliberately not in the key. It used to be, because the stored
// response carries `access-control-allow-origin` and the Cache API ignores
// `Vary`, so an entry stored under one origin's header would have been served,
// header and all, to the next. That protected one header at the price of one
// upstream call per origin -- and for `/health/scopus`, cached precisely to
// guard a weekly allowance, a monitor with no `Origin` and a browser with one
// were two calls for one answer. `serveCached` rebuilds the header instead.
function canonicalCacheKey(request, canonicalParams = {}) {
  const cacheUrl = new URL(`https://papertok.internal/cache${new URL(request.url).pathname}`);
  for (const [name, value] of Object.entries(canonicalParams)) {
    if (value !== null && value !== undefined && value !== '') {
      cacheUrl.searchParams.set(name, String(value));
    }
  }
  return new Request(cacheUrl.toString(), { method: 'GET' });
}

// One stored body for every origin, with the one origin-dependent header put
// back for whoever is asking now. `vary: Origin` travels with it so the caches
// downstream of this one -- the browser's, any intermediary's -- do not
// cross-serve what this one no longer needs to keep apart.
function serveCached(cached, origin, env) {
  const headers = new Headers(cached.headers);
  headers.delete('access-control-allow-origin');
  headers.delete('vary');
  for (const [name, value] of Object.entries(corsHeaders(origin, env))) headers.set(name, value);
  return new Response(cached.body, { status: cached.status, headers });
}
```

- [ ] **Step 4: Los seis llamadores**

En cada uno, dos ediciones. `canonicalCacheKey(request, origin, X)` → `canonicalCacheKey(request, X)`, y `if (cached) return cached;` → `if (cached) return serveCached(cached, origin, env);`:

| Sitio | Antes | Después |
|---|---|---|
| `cacheResponse` (:494) | `canonicalCacheKey(request, origin, options.canonicalParams)` | `canonicalCacheKey(request, options.canonicalParams)` |
| `handleArxiv` (:971) | `canonicalCacheKey(request, origin, Object.fromEntries(upstreamUrl.searchParams))` | `canonicalCacheKey(request, Object.fromEntries(upstreamUrl.searchParams))` |
| relay `/openalex/*` (:2005) | `canonicalCacheKey(request, origin, Object.fromEntries(target.searchParams))` | `canonicalCacheKey(request, Object.fromEntries(target.searchParams))` |
| `/health/email` (:2349) | `canonicalCacheKey(request, origin)` | `canonicalCacheKey(request)` |
| `/health/openalex` (:2386) | `canonicalCacheKey(request, origin)` | `canonicalCacheKey(request)` |
| `/health/scopus` (:2405) | `canonicalCacheKey(request, origin)` | `canonicalCacheKey(request)` |

Comprobar con `grep -n "canonicalCacheKey(" worker/report-api.js` que no queda ninguna llamada con tres argumentos y con `grep -n "if (cached) return cached" worker/report-api.js` que no queda ningún `return cached` pelado.

- [ ] **Step 5: Verde**

Run: `node --test worker/report-api.test.js 2>&1 | grep -E "^ℹ (tests|pass|fail)"`
Expected: `tests 103`, `pass 103`, `fail 0`. El test `keeps two different PubMed queries…` sigue en verde: dos consultas siguen siendo dos entradas.

- [ ] **Step 6: Mutación, una por aserción**

(a) Volver a poner `cacheUrl.searchParams.set('_origin', origin || 'no-origin');` en la clave (con el parámetro `origin` de vuelta en la firma y en los seis llamadores) → `upstreamCalls` = `3`, rojo. Restaurar. (b) En `serveCached`, devolver `cached` tal cual sin reconstruir → `www` recibe la cabecera de quien pobló la entrada (`null`, porque fue `bare`), rojo en la aserción de `www`. Restaurar.

- [ ] **Step 7: Commit**

```bash
git add worker/report-api.js worker/report-api.test.js
git commit -m "perf(worker): one cache entry serves every origin, with its own CORS header put back at serve time

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 2: Las puertas devuelven asientos, y `cacheResponse` es el único que los devuelve (L1, L4, L5)

**Files:**
- Modify: `worker/report-api.js` — `reserveProtectedProviderQuota` (:321-352), `awaitSharedPace` (:467-488), `cacheResponse` (:493-522), una función nueva `releaseHeld` y otra `reserveGates`
- Test: `worker/report-api.test.js`

**Interfaces:**
- Consumes: `releaseRequestQuota` (ya importado desde el plan anterior), `reserveSharedMinuteQuota(ceiling, env, origin)` → `{ error } | { reservation }` (ya existe).
- Produces: `reserveProtectedProviderQuota(identity, env, origin)` → `{} | { error: Response } | { reservation }`; `awaitSharedPace(ceiling, env, origin)` → `Response | null` (**pierde** `minuteReservation`); `releaseHeld(env, held)`; `reserveGates(origin, env, options, ceiling, held)` → `Response | null`. Task 3 envuelve `reserveGates` en un `try`; Task 4 cambia una línea dentro.

**Por qué.** Tres de las cuatro puertas devuelven un veredicto y tiran el asiento que reservaron; solo el compás sabe devolver el asiento del minuto, y solo ese. Todo lo que sea «una puerta gastó y una posterior rechazó» pierde el asiento. La más frecuente es **L4**: el compás rechaza en cuanto hay contención — la condición normal a 1 RPS — y cada rechazo resta una unidad de las 60 por minuto del usuario en `/related` **sin haber enviado nada**. Un lector activo puede agotar su minuto sin una sola llamada al proveedor, y el síntoma que ve apunta a otro sitio.

- [ ] **Step 1: La fixture y los tests**

Añadir a `worker/report-api.test.js`, junto a `actionRecordingLedger`:

```js
// One fake ledger for every quota-accounting test. It is told, per period key,
// what to do -- `refuse` answers not-accepted, `unavailable` answers non-2xx
// (what `callRequestQuotaLedger` reports as QUOTA_LEDGER_UNAVAILABLE), `broken`
// throws (a Durable Object that cannot be reached at all) -- and it records
// every reserve and every release with the key it was aimed at, so a test can
// assert that a refund names exactly the key and subject the reserve named.
function scriptedQuotaLedger(state, { refuse = () => false, unavailable = () => false, broken = () => false } = {}) {
  let lastName = '';
  return {
    idFromName: name => { lastName = String(name); return `quota-${lastName}`; },
    get: () => ({
      fetch: async (url, options) => {
        const action = String(url).split('/').pop();
        const body = JSON.parse(options.body);
        state.actions.push({ action, periodKey: lastName, subjectKey: body.subjectKey });
        if (action === 'release') return new Response(JSON.stringify({ released: true }));
        if (broken(lastName)) throw new Error(`ledger unreachable: ${lastName}`);
        if (unavailable(lastName)) return new Response('down', { status: 500 });
        if (refuse(lastName)) return new Response(JSON.stringify({ accepted: false, scope: 'user' }));
        return new Response(JSON.stringify({ accepted: true }));
      },
    }),
  };
}

// The period keys the gates use, as predicates: the minute ceiling is
// `s2:<minute>`, the beat is `s2:pace`, identity is `provider:<minute>`, and
// OpenAlex is `openalex:<minute>` then `openalex:day:<date>`.
const IS_MINUTE = key => key.startsWith('s2:') && !key.endsWith(':pace');
const IS_PACE = key => key.endsWith(':pace');
const IS_IDENTITY = key => key.startsWith('provider:');
const IS_OPENALEX_MINUTE = key => key.startsWith('openalex:') && !key.includes(':day:');
const IS_OPENALEX_DAY = key => key.startsWith('openalex:day:');

const releasesOf = state => state.actions.filter(a => a.action === 'release');

// A refund is a refund only if it names the key and the subject the reserve
// named: a recomputed key credits the next period, a different subject credits
// somebody else.
function assertRefunded(state, isKey) {
  const reserve = state.actions.find(a => a.action === 'reserve' && isKey(a.periodKey));
  const release = releasesOf(state).find(a => isKey(a.periodKey));
  assert.ok(reserve, `no reserve matched: ${JSON.stringify(state.actions)}`);
  assert.ok(release, `no release matched: ${JSON.stringify(state.actions)}`);
  assert.equal(release.periodKey, reserve.periodKey);
  assert.equal(release.subjectKey, reserve.subjectKey);
}

const RELATED_URL = 'https://papertok-report-api.example/related?paper_id=ARXIV:2607.12345';
const relatedThrough = (ledger, upstream = async () => new Response(
  JSON.stringify({ recommendedPapers: [] }), { headers: { 'content-type': 'application/json' } },
)) => withWorkerFetchMock(upstream, () => withCachedIdentity(() => reportApi.fetch(new Request(RELATED_URL, {
  headers: { origin: 'https://mugar123.github.io', authorization: 'Bearer test-token' },
}), { ...AUTHENTICATED_ENV, REQUEST_QUOTA_LEDGER: ledger })));

// L1: the identity gate refuses after the minute unit was already taken.
test('gives the minute unit back when the identity quota refuses after it', async () => {
  const state = { actions: [] };
  const response = await relatedThrough(scriptedQuotaLedger(state, { refuse: IS_IDENTITY }));

  assert.equal(response.status, 429);
  assert.equal((await response.json()).code, 'PROVIDER_RATE_LIMITED');
  assertRefunded(state, IS_MINUTE);
  assert.equal(releasesOf(state).length, 1, 'only the minute unit was held');
});

// L4: the beat refuses after both the minute unit and the identity unit were
// taken. The beat used to refund the minute on its own and had no way to know
// the identity unit existed; now the owner of both refunds both.
test('gives the minute unit and the identity unit back when the beat refuses after them', async () => {
  const state = { actions: [] };
  const response = await relatedThrough(scriptedQuotaLedger(state, { refuse: IS_PACE }));

  assert.equal(response.status, 429);
  assert.equal((await response.json()).code, 'PROVIDER_RATE_LIMITED');
  assertRefunded(state, IS_MINUTE);
  assertRefunded(state, IS_IDENTITY);
  assert.equal(releasesOf(state).length, 2);
});

// L5: the OpenAlex budget refuses after the identity unit was taken. Trends has
// no minute ceiling entry, so the identity unit is the only thing at risk.
test('gives the identity unit back when the OpenAlex budget refuses after it', async () => {
  const state = { actions: [] };
  const response = await withWorkerFetchMock(trendsPage, () => withCachedIdentity(() => reportApi.fetch(
    new Request(TRENDS_BASE, { headers: SIGNED_IN }),
    { ...AUTHENTICATED_ENV, REQUEST_QUOTA_LEDGER: scriptedQuotaLedger(state, { refuse: IS_OPENALEX_MINUTE }) },
  )));

  assert.equal(response.status, 429);
  assertRefunded(state, IS_IDENTITY);
  assert.equal(releasesOf(state).length, 1);
});

// The accepted path gives nothing back: every unit taken was spent on a real
// provider call. A refund here would mint quota out of nothing.
test('gives nothing back when every gate accepts', async () => {
  const state = { actions: [] };
  const response = await relatedThrough(scriptedQuotaLedger(state));

  assert.equal(response.status, 200);
  assert.equal(releasesOf(state).length, 0, JSON.stringify(state.actions));
});
```

- [ ] **Step 2: Verlos fallar**

Run: `node --test worker/report-api.test.js 2>&1 | grep -E "^(✖|ℹ fail)"`
Expected: `fail 3` — L1, L4 y L5 sin ninguna devolución (o solo una en L4, la del compás de antes). El de «gives nothing back» ya pasa y se queda de guardia.

- [ ] **Step 3: `reserveProtectedProviderQuota` devuelve lo que reservó**

Sustituir la función entera por:

```js
// Returns the ledger request it reserved with, not just a verdict: a later gate
// may refuse this caller after this unit is spent, and the refund has to name
// this unit -- this minute, this user -- rather than one recomputed later.
async function reserveProtectedProviderQuota(identity, env, origin) {
  if (!identity) return {};
  const minute = new Date().toISOString().slice(0, 16);
  const ledgerRequest = {
    periodKey: `provider:${minute}`,
    subject: `provider:${identity.uid}`,
    subjectLimit: boundedLimit(
      env.PROVIDER_USER_MINUTE_LIMIT,
      DEFAULT_PROVIDER_USER_MINUTE_LIMIT,
      500,
    ),
    globalLimit: boundedLimit(
      env.PROVIDER_GLOBAL_MINUTE_LIMIT,
      DEFAULT_PROVIDER_GLOBAL_MINUTE_LIMIT,
      100_000,
    ),
  };
  const reservation = await reserveRequestQuota(env.REQUEST_QUOTA_LEDGER, ledgerRequest);
  if (!reservation.accepted && reservation.code) {
    return { error: json({ code: 'PROVIDER_QUOTA_NOT_CONFIGURED' }, 503, {
      ...corsHeaders(origin, env),
      'cache-control': 'no-store',
    }) };
  }
  if (!reservation.accepted) {
    return { error: json({ code: 'PROVIDER_RATE_LIMITED' }, 429, {
      ...corsHeaders(origin, env),
      'cache-control': 'no-store',
      'retry-after': '60',
    }) };
  }
  return { reservation: ledgerRequest };
}
```

Comprobar con `grep -n "reserveProtectedProviderQuota(" worker/report-api.js` que `cacheResponse` es el único llamador. Si hay otro, parar y reportarlo: cambia la firma.

- [ ] **Step 4: `awaitSharedPace` deja de devolver por su cuenta**

Sustituir la función entera por:

```js
// The beat under the ceiling, for the providers that count per second. It runs
// last of the reserving gates because it is the only one that costs wall-clock:
// a caller the minute ceiling or the identity quota is about to turn away must
// not first take a second away from somebody who would have used it.
//
// It refunds nothing itself. The units the gates before it took are held by
// `reserveGates`, which gives every one of them back when this refuses -- the
// beat used to refund the minute unit on its own and had no way to know the
// identity unit existed.
async function awaitSharedPace(ceiling, env, origin) {
  if (!ceiling?.paced) return null;
  const slot = await awaitUpstreamSlot(env.REQUEST_QUOTA_LEDGER, { namespace: ceiling.namespace });
  if (slot.accepted) return null;
  if (slot.code) {
    return json({ code: 'PROVIDER_QUOTA_NOT_CONFIGURED' }, 503, {
      ...corsHeaders(origin, env),
      'cache-control': 'no-store',
    });
  }
  // The beat's own window, not a literal next to a comment that promised it.
  return json({ code: 'PROVIDER_RATE_LIMITED' }, 429, {
    ...corsHeaders(origin, env),
    'cache-control': 'no-store',
    'retry-after': PACE_RETRY_AFTER_SECONDS,
  });
}
```

- [ ] **Step 5: `releaseHeld`, `reserveGates`, y `cacheResponse` como dueño**

Justo antes de `cacheResponse`:

```js
// Every unit an earlier gate took goes back when a later gate says no: the
// caller made no provider call, and the ledger counts provider calls. Newest
// first, so a refund that fails leaves the oldest -- and cheapest to lose --
// unit as the leak. Same shape as `releaseAIQuota`: a refund that cannot be
// delivered is a leak, not a reason to change the answer the caller is owed.
async function releaseHeld(env, held) {
  for (const reservation of [...held].reverse()) {
    try {
      await releaseRequestQuota(env.REQUEST_QUOTA_LEDGER, reservation);
    } catch {
      // Leak, not error.
    }
  }
}

// The reserving gates, in order, each pushing what it took onto `held`. Returns
// the first refusal, or null when every gate accepted. The caller owns `held`
// and owns the refund; this function only knows what was taken.
async function reserveGates(origin, env, options, ceiling, held) {
  const minute = ceiling ? await reserveSharedMinuteQuota(ceiling, env, origin) : {};
  if (minute.error) return minute.error;
  if (minute.reservation) held.push(minute.reservation);
  const identity = await reserveProtectedProviderQuota(options.identity || null, env, origin);
  if (identity.error) return identity.error;
  if (identity.reservation) held.push(identity.reservation);
  const paceError = await awaitSharedPace(ceiling, env, origin);
  if (paceError) return paceError;
  if (options.openAlexCalls) {
    const budgetError = await reserveOpenAlexBudget(env, origin, options.openAlexCalls);
    if (budgetError) return budgetError;
  }
  return null;
}
```

Y en `cacheResponse`, sustituir desde `// Resolved once here…` hasta `if (budgetError) return budgetError; }` inclusive por:

```js
  const ceiling = SHARED_MINUTE_CEILINGS[new URL(request.url).pathname];
  const held = [];
  const refusal = await reserveGates(origin, env, options, ceiling, held);
  if (refusal) {
    await releaseHeld(env, held);
    return refusal;
  }
```

- [ ] **Step 6: Verde**

Run: `node --test worker/report-api.test.js 2>&1 | grep -E "^ℹ (tests|pass|fail)"`
Expected: `tests 107`, `pass 107`, `fail 0`. Los dos tests del plan anterior sobre la devolución del compás (`gives the minute back when the beat refuses…` y `charges /related and /sources/s2…`) siguen en verde: el comportamiento observable no cambia, solo quién lo hace.

- [ ] **Step 7: Mutación, una por propiedad**

(a) Quitar `await releaseHeld(env, held);` de `cacheResponse` → L1, L4 y L5 en rojo (`no release matched`). Restaurar. (b) Moverlo **fuera** del `if (refusal)`, para que corra también en el camino aceptado → `gives nothing back when every gate accepts` en rojo. Restaurar. (c) En `reserveProtectedProviderQuota`, devolver `{}` en vez de `{ reservation: ledgerRequest }` al aceptar → L4 en rojo en `assertRefunded(state, IS_IDENTITY)`; L1 sigue en verde (no llega a reservar identidad). Restaurar.

- [ ] **Step 8: Commit**

```bash
git add worker/report-api.js worker/report-api.test.js
git commit -m "fix(worker): every gate returns what it took, and cacheResponse alone gives it back on a refusal

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 3: Un `throw` entre puertas devuelve lo acumulado; el fetcher no (L2a, L2b, L3, y la decisión sobre L2c)

**Files:**
- Modify: `worker/report-api.js` — `reserveGates` (de Task 2)
- Test: `worker/report-api.test.js`

**Interfaces:**
- Consumes: `reserveGates`, `releaseHeld`, `scriptedQuotaLedger`, `assertRefunded`, `relatedThrough` de Task 2.
- Produces: nada nuevo; `reserveGates` gana un `try/catch`.

**Por qué.** Cada puerta llega a un Durable Object distinto (`s2:<minuto>`, `provider:<minuto>`, `s2:pace`) y cualquiera puede lanzar o responder no-ok con los anteriores ya aceptados. Un `throw` sube por `cacheResponse` hasta el `catch` del router, que responde 502, y nadie devuelve nada. Y el compás con `code` responde 503 por el mismo camino de Task 2 (un rechazo), así que L3 ya cae con lo anterior — este paso solo lo **prueba**. Lo que no se devuelve, a propósito, es tras el fetcher: un fetch que falla puede haber sido un envío.

- [ ] **Step 1: Los tests**

Añadir después de los de Task 2:

```js
// L2a: the identity gate's Durable Object cannot be reached, with the minute
// unit already taken. The throw reaches the router's catch and becomes a 502;
// the unit has to come back on the way.
test('gives the minute unit back when the identity ledger throws after it', async () => {
  const state = { actions: [] };
  const response = await relatedThrough(scriptedQuotaLedger(state, { broken: IS_IDENTITY }));

  assert.equal(response.status, 502);
  assertRefunded(state, IS_MINUTE);
  assert.equal(releasesOf(state).length, 1);
});

// L2b: the beat's Durable Object cannot be reached, with both units taken.
test('gives both units back when the pace ledger throws after them', async () => {
  const state = { actions: [] };
  const response = await relatedThrough(scriptedQuotaLedger(state, { broken: IS_PACE }));

  assert.equal(response.status, 502);
  assertRefunded(state, IS_MINUTE);
  assertRefunded(state, IS_IDENTITY);
  assert.equal(releasesOf(state).length, 2);
});

// L3: the beat's Durable Object answers but not-ok -- QUOTA_LEDGER_UNAVAILABLE
// -- which `awaitSharedPace` reports as a 503. `s2:pace` is a different object
// from `s2:<minute>`, so it can fail with the minute unit already accepted.
test('gives both units back when the pace ledger is unavailable after them', async () => {
  const state = { actions: [] };
  const response = await relatedThrough(scriptedQuotaLedger(state, { unavailable: IS_PACE }));

  assert.equal(response.status, 503);
  assert.equal((await response.json()).code, 'PROVIDER_QUOTA_NOT_CONFIGURED');
  assertRefunded(state, IS_MINUTE);
  assertRefunded(state, IS_IDENTITY);
  assert.equal(releasesOf(state).length, 2);
});

// Not a leak: a fetch that fails may still have reached the provider -- a
// timeout is a send that got no answer, a 429 is a send that counted -- and the
// ledger counts sends. Refunding here would let it count fewer sends than were
// made, and under sustained pressure exceed the one-a-second the beat exists
// to keep. So the units stay spent. This test is the decision, pinned.
test('gives nothing back when the fetcher fails, because the send may have happened', async () => {
  const state = { actions: [] };
  const response = await withWorkerFetchMock(
    async () => { throw new Error('provider hung up'); },
    () => reportApi.fetch(new Request(
      'https://papertok-report-api.example/sources/s2?q=malaria',
      { headers: { origin: 'https://mugar123.github.io' } },
    ), { REQUEST_QUOTA_LEDGER: scriptedQuotaLedger(state) }),
  );

  assert.equal(response.status, 502);
  assert.ok(state.actions.some(a => a.action === 'reserve' && IS_MINUTE(a.periodKey)), 'the minute unit was taken');
  assert.equal(releasesOf(state).length, 0, `a refund after the fetcher: ${JSON.stringify(releasesOf(state))}`);
});
```

- [ ] **Step 2: Verlos fallar**

Run: `node --test worker/report-api.test.js 2>&1 | grep -E "^(✖|ℹ fail)"`
Expected: `fail 2` — L2a y L2b (`no release matched`). L3 ya pasa (es un rechazo, Task 2 lo cubre) y el del fetcher ya pasa (no hay `try` todavía); los dos se quedan de guardia contra el Step 3.

- [ ] **Step 3: El `try` que termina antes del fetcher**

En `reserveGates`, envolver el cuerpo entero:

```js
// The reserving gates, in order, each pushing what it took onto `held`. Returns
// the first refusal, or null when every gate accepted; throws when a ledger
// cannot be reached, after giving back whatever was taken before it -- each
// gate is a different Durable Object, and any of them can fail with the
// earlier ones already spent. The fetcher is deliberately not inside this: a
// fetch that fails may still have reached the provider, and the ledger counts
// provider calls, not answers.
async function reserveGates(origin, env, options, ceiling, held) {
  try {
    const minute = ceiling ? await reserveSharedMinuteQuota(ceiling, env, origin) : {};
    if (minute.error) return minute.error;
    if (minute.reservation) held.push(minute.reservation);
    const identity = await reserveProtectedProviderQuota(options.identity || null, env, origin);
    if (identity.error) return identity.error;
    if (identity.reservation) held.push(identity.reservation);
    const paceError = await awaitSharedPace(ceiling, env, origin);
    if (paceError) return paceError;
    if (options.openAlexCalls) {
      const budgetError = await reserveOpenAlexBudget(env, origin, options.openAlexCalls);
      if (budgetError) return budgetError;
    }
    return null;
  } catch (error) {
    await releaseHeld(env, held);
    throw error;
  }
}
```

`cacheResponse` no cambia: el `throw` sigue subiendo hasta el router.

- [ ] **Step 4: Verde**

Run: `node --test worker/report-api.test.js 2>&1 | grep -E "^ℹ (tests|pass|fail)"`
Expected: `tests 111`, `pass 111`, `fail 0`.

- [ ] **Step 5: Mutación, en las dos direcciones**

(a) Quitar el `try/catch` (dejar el cuerpo pelado) → L2a y L2b en rojo. Restaurar. (b) Mover el `try` a `cacheResponse` de modo que abarque **también** `const payload = await fetcher();` y devuelva `held` en el `catch` → el test del fetcher en rojo (`releasesOf` = `2`: minuto y compás… nota: el compás no se «devuelve», solo el minuto; `releasesOf` = `1`). Restaurar. Este segundo mutante es la decisión de la auditoría: si alguien lo aplica creyendo que arregla una fuga, este test le dice por qué no.

- [ ] **Step 6: Commit**

```bash
git add worker/report-api.js worker/report-api.test.js
git commit -m "fix(worker): a ledger that throws between gates gives back what was taken; the fetcher never does

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 4: El presupuesto de OpenAlex devuelve el minuto cuando el día rechaza (L6)

**Files:**
- Modify: `worker/report-api.js` — `reserveOpenAlexBudget` (:1925-1978) y sus dos llamadores (`reserveGates` y el relay `/openalex/*` a :2010)
- Test: `worker/report-api.test.js`

**Interfaces:**
- Consumes: `releaseHeld` de Task 2.
- Produces: `reserveOpenAlexBudget(env, origin, amount)` → `{ error: Response } | { reservations: LedgerRequest[] }`. Dos llamadores; ambos cambian aquí.

**Por qué.** El comentario de la función lo dice: «Both orders can leave one bucket spent when the other refuses; this way the leak lives in the minute bucket». Era la fuga menos mala, elegida cuando devolver no era un patrón. Hoy son tres líneas. Y las reservas de OpenAlex tienen que viajar como las demás para que `reserveGates` pueda devolverlas si… no hay puerta después. Hoy no la hay; el contrato uniforme es lo que vale.

- [ ] **Step 1: Los tests**

```js
// L6: the OpenAlex day ceiling refuses after the minute ceiling accepted. The
// function used to leave the minute unit to expire -- "the leak lives in the
// minute bucket, which is thrown away sixty seconds later" -- as the lesser of
// two leaks, before giving a unit back was a pattern in this file.
test('gives the OpenAlex minute unit back when the day ceiling refuses after it', async () => {
  const state = { actions: [] };
  const response = await withWorkerFetchMock(trendsPage, () => withCachedIdentity(() => reportApi.fetch(
    new Request(TRENDS_BASE, { headers: SIGNED_IN }),
    { ...AUTHENTICATED_ENV, REQUEST_QUOTA_LEDGER: scriptedQuotaLedger(state, { refuse: IS_OPENALEX_DAY }) },
  )));

  assert.equal(response.status, 429);
  assert.equal(response.headers.get('retry-after'), '300', 'the day ceiling names its own wait');
  assertRefunded(state, IS_OPENALEX_MINUTE);
  // Trends also holds an identity unit, and `reserveGates` gives that back too.
  assertRefunded(state, IS_IDENTITY);
  assert.equal(releasesOf(state).length, 2);
});

// The relay reaches the budget without `cacheResponse`, so the refund inside
// the function is the only one it gets.
test('the /openalex relay gives the minute unit back when the day ceiling refuses', async () => {
  const state = { actions: [] };
  // Reuse the relay request the existing relay tests build (grep "openalex/works"
  // in this file); only the ledger differs.
  const response = await withWorkerFetchMock(
    async () => new Response('{"results":[]}', { headers: { 'content-type': 'application/json' } }),
    () => reportApi.fetch(new Request(
      RELAY_WORKS_URL,
      { headers: { origin: 'https://mugar123.github.io' } },
    ), { ...OPEN_ROUTE_ENV, REQUEST_QUOTA_LEDGER: scriptedQuotaLedger(state, { refuse: IS_OPENALEX_DAY }) }),
  );

  assert.equal(response.status, 429);
  assertRefunded(state, IS_OPENALEX_MINUTE);
  assert.equal(releasesOf(state).length, 1);
});
```

Donde `RELAY_WORKS_URL` es la URL que ya usa el test del relay más cercano a la línea que `grep -n "openalex/works" worker/report-api.test.js` señale; definirla como constante junto a `RELATED_URL` si no existe ya con otro nombre.

- [ ] **Step 2: Verlos fallar**

Run: `node --test worker/report-api.test.js 2>&1 | grep -E "^(✖|ℹ fail)"`
Expected: `fail 2` — ninguno de los dos ve la devolución de `openalex:<minuto>` (en el de trends, `releasesOf` es `1`: solo la identidad, que Task 2 ya devuelve).

- [ ] **Step 3: `reserveOpenAlexBudget` devuelve asientos, y el minuto por dentro**

Sustituir la función entera, comentario incluido, por:

```js
// Every OpenAlex call this Worker pays for goes through here, and it is the only
// thing standing between a scripted caller and the day's budget: the Origin gate
// is advisory by construction -- a request with no `Origin` header at all has
// nothing to check, and one is trivially forged -- so a ceiling is the frontier,
// not the header. The guest feed reads OpenAlex without a session, so the ceiling
// is global rather than per user, and it is reserved only after a cache miss so
// repeated queries cost nothing.
//
// Minute first, day second, and the minute given back if the day says no. This
// used to leave the minute unit to expire as the lesser of two leaks, chosen
// before giving a unit back was a pattern here; now nothing is left spent for
// a call that was never made. What was taken travels back to the caller, the
// same shape as every other gate, so whoever owns the refund can name it.
async function reserveOpenAlexBudget(env, origin, amount) {
  const now = new Date().toISOString();
  const periods = [
    [`openalex:${now.slice(0, 16)}`, boundedLimit(
      env.OPENALEX_GLOBAL_MINUTE_LIMIT,
      DEFAULT_OPENALEX_GLOBAL_MINUTE_LIMIT,
      100_000,
    ), '60'],
    // Not the true seconds until UTC midnight: a `retry-after` of several hours is
    // a client that stops asking until someone reloads the tab, and the daily
    // ceiling is an emergency brake, not a normal state.
    [`openalex:day:${now.slice(0, 10)}`, boundedLimit(
      env.OPENALEX_GLOBAL_DAILY_LIMIT,
      DEFAULT_OPENALEX_GLOBAL_DAILY_LIMIT,
      1_000_000,
    ), '300'],
  ];
  const taken = [];
  for (const [periodKey, limit, retryAfter] of periods) {
    const ledgerRequest = {
      periodKey,
      subject: 'openalex:shared',
      subjectLimit: limit,
      globalLimit: limit,
      amount,
    };
    const reservation = await reserveRequestQuota(env.REQUEST_QUOTA_LEDGER, ledgerRequest);
    if (!reservation.accepted) {
      await releaseHeld(env, taken);
      if (reservation.code) {
        return { error: json({ code: 'PROVIDER_QUOTA_NOT_CONFIGURED' }, 503, {
          ...corsHeaders(origin, env),
          'cache-control': 'no-store',
        }) };
      }
      return { error: json({ code: 'PROVIDER_RATE_LIMITED' }, 429, {
        ...corsHeaders(origin, env),
        'cache-control': 'no-store',
        'retry-after': retryAfter,
      }) };
    }
    taken.push(ledgerRequest);
  }
  return { reservations: taken };
}
```

`releaseHeld` está definido más abajo en el fichero (junto a `cacheResponse`); es `async function`, se iza.

- [ ] **Step 4: Los dos llamadores**

En `reserveGates`, las dos líneas del presupuesto pasan a:

```js
    if (options.openAlexCalls) {
      const budget = await reserveOpenAlexBudget(env, origin, options.openAlexCalls);
      if (budget.error) return budget.error;
      held.push(...budget.reservations);
    }
```

En el relay (`grep -n "reserveOpenAlexBudget(env, origin, OPENALEX_CALLS.relay)" worker/report-api.js`):

```js
  const budget = await reserveOpenAlexBudget(env, origin, OPENALEX_CALLS.relay);
  if (budget.error) return budget.error;
```

Comprobar con `grep -n "reserveOpenAlexBudget(" worker/report-api.js` que no queda un tercer llamador.

- [ ] **Step 5: Verde y suite completa**

Run: `node --test worker/report-api.test.js 2>&1 | grep -E "^ℹ (tests|pass|fail)"`
Expected: `tests 113`, `pass 113`, `fail 0`. Los tests existentes de OpenAlex (`two discarded trend filters are one billed query`, los del relay, los de `/health/openalex`) siguen en verde.

Run: `npm test 2>&1 | grep -E "^ℹ (tests|pass|fail|cancelled)"` y `npm run lint`
Expected: `1988` (1977 + 1 + 4 + 4 + 2), `fail 0`, `cancelled 0`; lint limpio.

- [ ] **Step 6: Mutación**

Quitar `await releaseHeld(env, taken);` del bucle → los dos tests nuevos en rojo (`no release matched` para `IS_OPENALEX_MINUTE`). Restaurar.

- [ ] **Step 7: Commit**

```bash
git add worker/report-api.js worker/report-api.test.js
git commit -m "fix(worker): the OpenAlex budget gives the minute back when the day refuses, instead of choosing the lesser leak

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 5: Documentación

**Files:**
- Modify: `STATE.md` — entrada nueva de cabecera
- Modify: `worker/README.md` — el párrafo del compás y el de `/openalex/*`

**Interfaces:** ninguna.

- [ ] **Step 1: El README**

En el párrafo del compás (`grep -n "gives back the minute unit" worker/README.md`), sustituir la frase que empieza «That local refusal also gives back the minute unit…» por:

```
Any gate that refuses -- the minute ceiling, the identity quota, the beat, the
OpenAlex budget -- gives back every unit an earlier gate had already taken for
this request, and so does a ledger that cannot be reached between them:
`cacheResponse` holds what was taken and is the one place that refunds it. What
is never refunded is a failed fetch: a timeout is a send the provider received
and did not answer, and the ledger counts sends, not answers.
```

En el párrafo de `/openalex/*` (`grep -n "reserved only after an edge-cache miss" worker/README.md`), añadir al final:

```
Minute first, day second, and the minute is given back when the day refuses.
```

Y donde el README describa la caché de borde (`grep -n "edge cache" worker/README.md`, la primera mención), añadir una frase:

```
One cached entry serves every allowed origin; the CORS header is put back for
whoever is asking, so a monitor without an `Origin` and a browser with one no
longer cost two provider calls for one answer.
```

- [ ] **Step 2: `STATE.md`**

Encima de la entrada más reciente:

```markdown
## Caché compartida y un solo dueño de las devoluciones (2026-09-0X)

**Cerrado S6 y cinco de las «seis fugas» — plan en
`docs/superpowers/plans/2026-09-03-s2-abiertos.md`, auditoría en
`docs/AUDITORIA-S2-ABIERTOS-2026-09-03.md`.** La caché de borde ya no se parte
por origen: la clave pierde `_origin` y `serveCached` reconstruye al servir la
única cabecera que variaba, con `vary: Origin` para que las cachés de abajo
tampoco crucen; seis sitios, un helper. Donde dolía: `/health/scopus`,
cacheada para proteger el cupo semanal, costaba una llamada por origen. Y las
puertas de `cacheResponse` cambian de contrato: cada una devuelve *lo que
reservó* y `reserveGates` acumula; ante un rechazo **o un `throw`** de una
puerta posterior, `cacheResponse` devuelve todo lo acumulado — el mismo
asiento, no uno recalculado — y `awaitSharedPace` deja de devolver por su
cuenta. `reserveOpenAlexBudget` devuelve el minuto cuando el día rechaza, en
vez de elegir «la fuga menos mala» como decía su comentario. Ningún número que
vea un cliente cambia.

**Una de las seis no era fuga, y hay un test que lo dice.** Un `throw` del
fetcher llega después de enviar, o intentar enviar, al proveedor: un timeout es
un envío sin respuesta. El ledger cuenta envíos; devolver ahí contaría de
menos y, bajo presión, superaría el 1 RPS que el compás administra. El `try`
que devuelve asientos termina antes del fetcher, y `gives nothing back when
the fetcher fails` pincha esa decisión para que nadie la «arregle».

Implementado, probado (suite entera N/N, cero `cancelled`) y revisado — no
desplegado todavía. Solo cambia el Worker. Sigue abierto, y no es código: la
solicitud a Semantic Scholar de un límite mayor que 1 RPS, pendiente de @mugar.
```

Sustituir `0X` por el día y `N/N` por el número medido.

- [ ] **Step 3: Suite y commit**

Run: `npm test 2>&1 | grep -E "^ℹ (tests|pass|fail|cancelled)"`
Expected: `fail 0`, `cancelled 0`.

```bash
git add STATE.md worker/README.md
git commit -m "docs(s2): shared cache entries and one owner of every refund, recorded

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

## Auto-revisión

**Cobertura.** C1/S6 → Task 1. L1, L4, L5 → Task 2. L2a, L2b, L3 → Task 3 (L3 cae con el mecanismo de Task 2 y Task 3 lo prueba). L2c → Task 3 lo pincha como *no fuga*, por decisión de la auditoría. L6 → Task 4. El límite de 1 RPS no es código y queda fuera.

**Consistencia.** `canonicalCacheKey(request, canonicalParams)` y `serveCached(cached, origin, env)` se definen y se usan solo en Task 1. `reserveProtectedProviderQuota` → `{} | { error } | { reservation }` y `awaitSharedPace(ceiling, env, origin)` se definen en Task 2 y no cambian después. `reserveGates(origin, env, options, ceiling, held)` nace en Task 2 sin `try`, Task 3 le añade el `try`, Task 4 cambia sus dos líneas de OpenAlex; el cuerpo de Task 3 reproduce el de Task 2 verbatim más el envoltorio, y Task 4 solo toca el bloque `if (options.openAlexCalls)`. `releaseHeld(env, held)` nace en Task 2 y Task 4 lo llama desde `reserveOpenAlexBudget`, que está antes en el fichero — `async function` se iza. `reserveOpenAlexBudget` → `{ error } | { reservations }` se define en Task 4 y sus dos llamadores cambian en la misma tarea. La fixture `scriptedQuotaLedger`, los predicados `IS_*`, `releasesOf`, `assertRefunded`, `RELATED_URL` y `relatedThrough` nacen en Task 2 y los usan 3 y 4.

**Recuentos.** `report-api.test.js`: 102 → 103 (T1) → 107 (T2) → 111 (T3) → 113 (T4). Suite: 1977 → 1978 → 1982 → 1986 → 1988. Si `main` avanzó antes de empezar, medir y desplazar.

**Lo que este plan no hace.** No cambia ningún `retry-after`, ningún techo, ni el orden de las puertas. No toca `upstream-pace.js`. No devuelve nada tras el fetcher, y tiene un test que lo impide.
